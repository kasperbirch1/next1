import HeroSlider from "../components/HeroSlider";
import Nav from "./Nav";

const Header = () => {
  return (
    <header>
      <Nav />
      <HeroSlider />
    </header>
  );
};

export default Header;
